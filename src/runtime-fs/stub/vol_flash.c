#include <runtime-fs/fs.h>

///////////////////////////////////////////////////////////////////////////////
// PUBLIC API (STUB)

/**
 * @brief Initializes a flash-based volume.
 */
fs_volume_t *fs_vol_init_flash(size_t size) {
  (void)size;
  return NULL; // Not supported in this platform
}
