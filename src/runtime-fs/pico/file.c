#include <runtime-fs/fs.h>

/**
 * @brief Initializes a filesystem-based volume.
 */
fs_volume_t *fs_vol_init_file(const char *path, size_t size) {
  (void)path;
  (void)size;
  return NULL; // Not supported in this platform
}